┌─────────────────────────────────────────────────────────────────────┐
│                        ✅ ГОТОВО                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ПОДГОТОВКА:                                                        │
│  ├── .env — настройки БД и API ключи                                │
│  ├── requirements.txt — зависимости Python                          │
│  └── Обновлены импорты в скриптах (config → .env)                  │
│                                                                      │
│  BACKEND API (FastAPI):                                              │
│  ├── api/database.py — подключение к PostgreSQL                     │
│  ├── api/main.py — точка входа, CORS настроен                       │
│  │                                                                   │
│  ├── api/models/                                                     │
│  │   ├── instrument.py — модель инструментов                        │
│  │   ├── candle.py — модель свечей                                  │
│  │   └── open_interest.py — модель OI                               │
│  │                                                                   │
│  ├── api/schemas/                                                    │
│  │   ├── instrument.py — JSON схемы инструментов                    │
│  │   ├── candle.py — JSON схемы свечей                              │
│  │   └── open_interest.py — JSON схемы OI                           │
│  │                                                                   │
│  └── api/routers/                                                    │
│      ├── instruments.py — /api/instruments/*                        │
│      ├── candles.py — /api/candles/{sec_id}                         │
│      ├── open_interest.py — /api/openinterest/{sectype}             │
│      └── chart.py — /api/chart/{sec_id}                             │
│                                                                      │
│  РАБОТАЮЩИЕ ENDPOINTS:                                               │
│  ├── GET /api/instruments — список (388 шт)                         │
│  ├── GET /api/instruments/groups — группы                           │
│  ├── GET /api/instruments/search?q= — поиск                         │
│  ├── GET /api/instruments/{sec_id} — один инструмент                │
│  ├── GET /api/candles/{sec_id} — свечи                              │
│  ├── GET /api/openinterest/{sectype} — открытый интерес             │
│  └── GET /api/chart/{sec_id} — свечи + OI вместе                    │
│                                                                      │
│  ЗАПУСК: uvicorn api.main:app --reload                              │
│  ДОКУМЕНТАЦИЯ: http://localhost:8000/docs                           │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                   📋 TODO: ПЕРЕД ДЕПЛОЕМ                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  1. CORS — указать домен фронтенда                                  │
│     Файл: api/main.py                                               │
│     Изменить: allow_origins=["*"]                                   │
│     На: allow_origins=["https://твой-сайт.ru"]                      │
│                                                                      │
│  2. .env на сервере — другой пароль БД                              │
│     Создать новый .env с production паролем                         │
│                                                                      │
│  3. HTTPS — настроить SSL сертификат                                │
│     Через NGINX или Let's Encrypt                                   │
│                                                                      │
│  4. Systemd/Supervisor — автозапуск API                             │
│     Чтобы API запускался после перезагрузки сервера                 │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                📋 TODO: КОГДА ПОНАДОБИТСЯ                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  • Логирование — если нужно искать баги на сервере                  │
│  • Кэширование (Redis) — если API тормозит                          │
│  • Rate limiting — если много пользователей / спам                  │
│  • Docker — для удобного деплоя                                     │
│  • Авторизация (JWT) — если нужны пользователи                      │
│  • WebSocket — если нужен realtime                                  │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘


cd C:\Users\Vadim\PycharmProjects\MOEX\frontend
npm run dev

uvicorn api.main:app --reload