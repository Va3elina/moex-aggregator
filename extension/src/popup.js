/* Фрейм popup — пока статичный.
 * TODO(auth): логин во Фрейм (OAuth) → выдача короткоживущего embed-токена,
 * хранение в chrome.storage.local, проброс в iframe widget'а (гейтинг по тарифу).
 * TODO(settings): тумблеры видимости вкладок, выбор темы по умолчанию.
 */
