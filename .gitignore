# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Node.js
node_modules/
frontend/dist/

# IDE
.idea/

# Секреты и логи
.env
.env.*
!.env.example
logs/

# Деплой-заметки с паролями
DEPLOY_NOTES.md

# Временные
*.log
.DS_Store
Thumbs.db
nul

# Cloudflare Tunnel (бинарник скачивается отдельно)
cloudflared.exe
tunnel.log

# Бэкапы БД
backups/

# Данные (Excel, CSV, JSON-кэш)
*.xlsx
*.csv
buffett_tmp*

# AI assistants
.claude/*
!.claude/skills/
!.claude/agents/
.gemini/
.antigravityignore

# IDE run configs
.run/

# Внешние репо
ml4t_repo/

# Заметки (не код)
Заметки/
сила рынка/
тз на баффета/
трендевушка/

# Старые скрипты
btc_quant_lab.py
crypto_breadth*.pine
russia_breadth.pine
excel_content.json
render.yaml
restart_hourly.sh
start.ps1
start.sh
stop.ps1
stop.sh
deploy.sh

# Бэктесты и стратегии (не часть сайта)
backtest_*.py
backtest_*.json
backtest_*.png
pinescript_strategy.pine
STRATEGY_PARAMS.md
compare_stocks.py

# Локальные артефакты разработки (v105 session)
.dev-pids/
cbonds_capture.flow
# scripts/ — раньше игнорилось целиком, но build-sprite.py нужен Docker build'у
# для сборки sprite.png из логотипов. Игнорим только тяжёлые/локальные части:
scripts/landing-videos/
scripts/*.flow
scripts/__pycache__/
*.pdf
frontend/.claude/
extension/store/build/
extension/store/screenshots/*.png

# Рантайм-данные (manual SCHA PDF через docker volume, signal scratch) — не код.
# Якорь от корня обязателен: голое data/ игнорило и frontend/src/data (туры, методология)
/data/
signals/data/
# Бэкап-копии файлов (создаются при правках на сервере как .bak)
*.bak
# Стухший ручной SW (реальный — frontend/public/sw.js с __SW_VERSION__)
frontend/src/pages/sw.js
