-- Найдено 2026-07-14 (сессия 2): аудит миграции 024 проверял на «это акция?»
-- ТОЛЬКО коды из старого OI_ASSETS (65) — а не весь реальный набор sectype в
-- open_interest (118). NV/IS/MV в OI_ASSETS никогда не было, поэтому аудит
-- 024 их физически не рассматривал — не отбраковал, а просто не увидел.
--
-- Живой self-join с instruments (WHERE sec_id=sectype, тот же принцип что 023/024):
--   NV → NVTK «НОВАТЭК» — есть настоящая строка-акция (sec_id=sectype=NVTK) → добавляем.
--   IS → «Артген» — ЕСТЬ фьючерсы (ISU/ISH/ISZ/ISM), НО строки-акции с
--     sec_id=sectype нет вообще — та же честная дыра данных, что и у Мечела/
--     РуссНефти/Софтлайна/Сегежи/Whoosh/СПБ биржи (024). НЕ добавляем, не
--     выдумываем тикер.
--   MV → «М.Видео» — аналогично IS: фьючерсы есть (MVH/MVZ/MVM/MVU), строки-акции
--     нет. НЕ добавляем.
--   CK → «Северсталь (мини)» — дубль уже смэпленной CHMF→CH (как GZ/SR в 024).
--     НЕ добавляем.
--
-- Итого честный список «реальная компания с ОИ, но без строки-акции в
-- instruments» расширяется с 6 до 8: Мечел, РуссНефть, Софтлайн, Сегежа,
-- Whoosh, СПБ биржа, Артген, М.Видео.

INSERT INTO ticker_futures_map (stock_ticker, futures_sectype, display_name, source) VALUES
    ('NVTK', 'NV', 'НОВАТЭК', 'validated_2026-07-14b')
ON CONFLICT (stock_ticker) DO NOTHING;
