/**
 * Фиче-флаг Telegram-алертов.
 *
 * ВРЕМЕННО ВЫКЛЮЧЕНО (2026-06-07): доставка в Telegram с прода не работает — у хоста
 * пропал глобальный IPv6 (`2a03:6f00:a::2:a9f`), а IPv4 до Telegram блокирует РКН. Пока
 * это не починено, прячем UI алертов (колокол на индикаторе + секция в ЛК), чтобы не
 * вводить людей в заблуждение (они бы создали алерт / подключили Telegram, а ничего не
 * пришло бы). Бэкенд/бот/eval-loop остаются на месте и оживут сами, как только IPv6
 * вернётся — тогда просто верни ALERTS_ENABLED = true.
 *
 * См. HANDOFF.md и память signal_engine.md (раздел про инцидент IPv6).
 */
export const ALERTS_ENABLED = false;
