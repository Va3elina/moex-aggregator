/**
 * Фиче-флаг Telegram-алертов.
 *
 * ВКЛЮЧЕНО (2026-06-08): IPv6 хоста восстановлен (статический адрес `2a03:6f00:a::2:a9f`
 * + дефолт-маршрут прописаны в netplan `52-static-ipv6.yaml`) → Telegram снова достижим,
 * бот poll'ит, пуши уходят. UI алертов виден: колокол на индикаторе + секция в ЛК.
 *
 * Если Telegram снова отвалится (Timeweb отзовёт IPv6 / РКН-эскалация) — поставь false,
 * чтобы спрятать UI; durable-вариант на будущее — релей (Cloudflare Worker / VPS abroad).
 * См. HANDOFF.md и память signal_engine.md (инцидент IPv6 + фикс).
 */
export const ALERTS_ENABLED = true;

/**
 * Фиче-флаг сигналов по аномальным потокам фондов («Деньги в фондах»).
 *
 * ВЫКЛЮЧЕНО (2026-08-30): на бэке сигналы погашены переменной
 * FUNDS_FLOW_ALERTS_ENABLED=0 в /opt/frame/.env (см. signals/config.py), и
 * кнопка «Сигнал по фондам» обещала бы уведомления, которые не придут.
 * Прячем вход в конструктор на десктопе и мобиле.
 *
 * Существующие подписки НЕ трогаются: они остаются active и видны в личном
 * кабинете, поэтому список алертов там не скрываем — пользователь должен
 * видеть и мочь удалить то, что уже создал.
 *
 * Включать ПАРОЙ с бэковым флагом: сначала FUNDS_FLOW_ALERTS_ENABLED=1 в
 * .env, затем true здесь — иначе кнопка вернётся раньше рассылки.
 */
export const FUND_ALERTS_ENABLED = false;
