/**
 * OIMethodologyPage — методология «Открытые позиции».
 *
 * Коротко: что это за индикатор и как его читать. Без описания кнопок,
 * без параметров расчёта и порогов — только смысл.
 */
import { BarChart3 } from 'lucide-react';
import { useTranslation } from 'react-i18next';
import { MethodologyWrapper, Section, Interpretation, ReplayTourButton } from './shared';
import { NetPositionFigure, PerekosAxisFigure } from './infographics';

export default function OIMethodologyPage() {
  const { t } = useTranslation();
  return (
    <MethodologyWrapper icon={BarChart3} title={t('Открытые позиции')} backTo="/oi">
      <Section title={t('Что это')}>
        <p>
          {t('Открытые позиции (открытый интерес) — количество контрактов, которые сейчас держат участники срочного рынка по инструменту. Когда позиции открывают — объём растёт, когда закрывают — снижается. Данные публикует Московская биржа раз в торговый день.')}
        </p>
        <p className="mt-3">
          {t('На графике сверху — цена, снизу — позиции: общий объём, позиции на покупку, на продажу и чистая позиция (разница между ними). Биржа считает физических и юридических лиц отдельно, поэтому их можно смотреть по отдельности и сравнивать.')}
        </p>
        <NetPositionFigure />
      </Section>

      <Section title={t('Скринер сигналов')}>
        <p>
          {t('Вторая вкладка проходит по всем фьючерсам сразу и показывает, где позиции за день заметно сдвинулись или дошли до редкого уровня. Главная величина — перекос: насколько чистая позиция группы смещена в одну сторону. «Комета» в ленте показывает, где толпа стоит сейчас и откуда пришла.')}
        </p>
        <PerekosAxisFigure />
      </Section>

      <Section title={t('Как читать')}>
        <p className="mb-4">
          {t('Открытые позиции — не сигнал «покупать или продавать», а мера вовлечённости участников. Смотрите их в связке с ценой:')}
        </p>
        <Interpretation
          rows={[
            {
              label: t('Цена растёт + позиции растут'),
              meaning: t('В движение входят новые покупатели — позиции открываются, а не закрываются.'),
            },
            {
              label: t('Цена падает + позиции растут'),
              meaning: t('Идёт набор коротких позиций — участники добавляют ставки на снижение.'),
            },
            {
              label: t('Цена растёт + позиции падают'),
              meaning: t('Закрытие коротких позиций или фиксация прибыли. Движение есть, но без новой ставки участников.'),
            },
            {
              label: t('Физлица и юрлица расходятся'),
              meaning: t('Когда две группы массово стоят в противоположных направлениях — иногда это совпадает со сменой направления. Гарантий это не даёт.'),
            },
          ]}
        />
      </Section>

      <Section title={t('Вводный тур')}>
        <div className="flex flex-wrap gap-3">
          <ReplayTourButton tourKey="oi" indicatorPath="/oi" label={t('Показать тур по графику')} />
          <ReplayTourButton tourKey="oi-screener" indicatorPath="/oi?tab=screener" label={t('Показать тур по скринеру')} />
        </div>
      </Section>
    </MethodologyWrapper>
  );
}
