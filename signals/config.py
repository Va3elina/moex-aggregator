"""Configuration for signal-engine.

Загружается при импорте: тянет .env из корня проекта (как api/database.py).
"""
import os
from pathlib import Path
from dotenv import load_dotenv

load_dotenv(Path(__file__).parent.parent / ".env")

# ─── Telegram ────────────────────────────────────────────
BOT_TOKEN = os.getenv("BOT_TOKEN", "").strip()
SIGNALS_CHANNEL_ID = int(os.getenv("SIGNALS_CHANNEL_ID", "0"))
ADMIN_USER_ID = int(os.getenv("ADMIN_USER_ID", "0"))

# ─── Detection ──────────────────────────────────────────
LOOKBACK_DAYS = 60           # окно для rolling z-score
Z_THRESHOLD = 2.5            # |z| > этого → сигнал
COOLDOWN_HOURS = 24          # не чаще одного сигнала в сутки на (asset, signal_type)
MIN_HISTORY_DAYS = 30        # минимум точек для статистически значимого z

# ─── Render (matplotlib chart) ──────────────────────────
CHART_PERIOD_DAYS = 365      # период, отображаемый на графике
CHART_WIDTH = 1280
CHART_HEIGHT = 720
SITE_URL = "framedata.ru"

# editorial-dark палитра (синхронизировано с frontend/src/index.css)
COLOR_BG = "#0a0a0a"         # paper-bg
COLOR_FG = "#f4f4f4"         # text primary
COLOR_MUTED = "#888"         # text muted
COLOR_GRID = "#1f1f1f"
COLOR_PRICE = "#f4f4f4"      # белая линия цены
COLOR_OI = "#FF5C2B"         # pumpkin accent — OI net positions
COLOR_MARKER = "#FF5C2B"     # красный кружок на последней точке

# ─── Assets ─────────────────────────────────────────────
# Исходные 65 тикеров фьючерсов MOEX с OI данными (валюта/сырьё/индексы + часть
# акций). Источник: OI/fetch_oi_5min_realtime.py:ALGOPACK_OI_TICKERS. Дублируем
# здесь чтобы не тянуть orchestrator-зависимости при импорте.
#
# ⚠️ Найдено 2026-07-14: этот список — НЕ источник истины, им был всегда
# только open_interest.sectype (118 живых кодов на сегодня). ticker_futures_map
# (024/026) свежее и шире по акциям (54), но этот список — то, что реально
# сканирует anomaly_scan.py (публичная лента аномалий: тосты/колокол + весь
# content-пайплайн через неё). Расхождение = слепая зона для этих активов.
#
# Валюта/сырьё/индексы (CR/Si/Eu/GL/SV/BR/NG/PT/PD/KC/CC/W4/IMOEXF/RI/RM и т.п.)
# остаются — это НЕ пробел, это структурно другой класс активов, у которого нет
# stock_ticker и который НЕ может попасть в ticker_futures_map по конструкции
# схемы (там акция↔фьючерс).
#
# ⚠️ Уточнено 2026-07-14 (проверено вживую через Algopack API): крипто-фьючерсы
# на MOEX ЕСТЬ — IB (Биткоин/IBIT, был здесь с самого начала) и BT (Биткоин
# Индекс МосБиржи, добавлен сегодня — реальные дневные OI-данные шли в БД
# месяцами, но anomaly_scan.py их не видел, т.к. не было в этом списке).
# Настоящие литеральные контракты BTC/ETH/SOL и междунар. акции (Alibaba/AMD/
# ASML/SAP/...), недавно появившиеся на FORTS (проверено через ISS), — НЕ
# добавлены: Algopack (наш источник разбивки по держателям) прямыми запросами
# подтверждённо не даёт по ним данных вообще, добавление в список ничего не
# даст. Перепроверять периодически, если Algopack расширит покрытие.
OI_ASSETS = [
    "CR", "CNYRUBF", "Si", "Eu", "IB", "VB", "USDRUBF", "GZ", "IMOEXF", "RB",
    "CC", "GL", "GLDRUBF", "NA", "NR", "ED", "GK", "SV", "SS", "X5",
    "MX", "MM", "NG", "GD", "SR", "SF", "GAZPF", "MN", "YD", "BR",
    "SE", "TN", "PT", "AF", "KC", "FF", "AL", "EURRUBF", "SBERF", "CE",
    "HS", "NK", "RI", "RL", "LK", "UC", "PD", "NM", "MC", "RM",
    "RN", "SP", "SN", "ME", "HY", "BM", "TT", "OJ", "MG", "W4",
    "DX", "CH", "MY", "VI", "AU", "BT",
    # + 29 акций из ticker_futures_map, отсутствовавших здесь (миграция 024,
    # найдено 2026-07-14): PX(Полюс) + 28 daily-only акций.
    "PX", "AK", "AS", "BN", "BS", "CM", "FE", "FL", "FS", "HD", "IR", "KM",
    "LE", "MT", "NB", "PH", "PI", "PS", "RA", "RD", "RT", "SC", "SG", "SH",
    "SO", "TB", "UN", "VK", "TP",
    # + 3 акции найденные 2026-07-14 (аудит 024 их не видел — их не было и в
    # ЭТОМ списке, поэтому аудит по OI_ASSETS их не мог найти). NV(Новатэк)
    # теперь и в ticker_futures_map (026). IS(Артген)/MV(М.Видео) в
    # ticker_futures_map НЕ входят (нет строки-акции в instruments — честная
    # дыра, см. 026), но здесь нужны для публичной ленты аномалий/тостов —
    # detector работает по sectype напрямую, stock_ticker ему не нужен.
    "NV", "IS", "MV",
]

# ─── Публичная лента аномалий (сайт: тосты + колокол) ───────────────────────
# Маркет-вайд скан (signals/anomaly_scan.py) гоняет ТЕ ЖЕ detector-функции, что и
# личные алерты (compute_position_atr / compute_fund_flow_atr), по всем активам и
# пишет аномалии ≥ порога в таблицу `anomalies` (scope='public'). Видимость НЕ
# делится по силе/тарифу — все видят одну ленту (решение Вадима), порог единый.
PUBLIC_RATIO_MIN = 3.0       # ×N к обычному дневному шагу — порог публичной аномалии
# Материальность фондов: страховка от шума мелких категорий («дырка→×14» на юане).
# Доля СЧА категории, ниже которой ATR считаем «замёрзшим» и аномалию не публикуем.
# 0 = выключено — стартуем без неё («пробуем ×3, смотрим объём»), включаем (напр.
# 0.0002), если мелкие категории зашумят.
PUBLIC_FUND_MIN_NAV_FRAC = 0.0

# ─── Kill-switch: сигналы аномальных потоков фондов ─────────────────────────
# Выключает ОБА потребителя fund-аномалий разом: персональные алерты
# (alerts_run, indicator='funds_flow' → Telegram/site/email) и публичную ленту
# тостов с колоколом (anomaly_scan, ветка категорий фондов).
#
# Подписки пользователей при этом НЕ трогаются: status в alerts остаётся
# 'active', ничего не удаляется и не паузится, поэтому обратное включение
# ничего не восстанавливает вручную — рассылка продолжится сама. Пропущенные
# за время паузы срабатывания не досылаются (это гейт на вычисление, а не
# очередь), но и «залпа» после включения не будет.
#
# НЕ влияет: OI-алерты и остальные индикаторы, а также уведомление «вышли
# новые данные по фондам» (signals/data_release_scan.py) — это про выход
# данных, а не про аномалию потока.
#
# Значение из .env на хосте (/opt/frame/.env), читают host-cron скрипты
# alerts_run.sh и anomaly_scan.sh: FUNDS_FLOW_ALERTS_ENABLED=0 — выключено.
FUNDS_FLOW_ALERTS_ENABLED = (
    os.getenv("FUNDS_FLOW_ALERTS_ENABLED", "1").strip().lower()
    not in ("0", "false", "no", "off", "")
)

# ─── Content-пайплайн («завод постов») ──────────────────────────────────────
CALENDAR_LOOKAHEAD_DAYS = 14   # на сколько дней вперёд смотрит MOEX-календарь
CONTENT_IMPORTANCE_MIN = 3     # порог значимости (Шаг А) для продолжения в pending
CONTENT_PENDING_DAYS = 5       # окно ожидания подтверждения данными (Шаг Б)

# Этап 9 — review-бот. Публикует через config.BOT_TOKEN (тот же бот, что и
# send_signal_post — у него нет активного getUpdates-поллера нигде, кроме
# content_review_bot.py, конфликта с alert_bot.py (ALERT_BOT_TOKEN, другой
# токен) нет). Карточки на ревью шлём в личку ADMIN_USER_ID.
# CONTENT_CHANNEL_ID — ТЕСТОВЫЙ канал на старте (не боевой @FrameTool) —
# Вадим создаёт канал, добавляет бота админом, даёт chat_id (обычно -100...).
CONTENT_CHANNEL_ID = int(os.getenv("CONTENT_CHANNEL_ID", "0"))
# users.id того же админа, что получает карточки — для reviewer_id (FK).
CONTENT_REVIEWER_USER_ID = int(os.getenv("CONTENT_REVIEWER_USER_ID", "0"))

# Коды причин ревью (миграция 054, research/content_pipeline_v2/). Ревью без
# машиночитаемой причины = потерянный размеченный пример: до 054 единственным
# каналом человеческого суждения был Вадим, дописывающий правило в промпт Шага В
# — из-за этого промпт и дорос до журнала багов на ~13 тыс. знаков.
# Список выведен из разбора 23 реальных черновиков (5 published / 9 rejected /
# 9 неотревьюенных), а НЕ придуман заранее — уточняется по мере разметки.
# Ключ уезжает в content_candidates.reviewer_reason_code (VARCHAR(32)),
# подпись — на кнопку в review-боте. Порядок = порядок кнопок.
REVIEW_REASON_CODES = [
    ("stretched_link",   "🪢 натянутая связь"),
    ("time_arrow",       "⏮ стрелка времени"),
    ("watery_conclusion", "💧 вода в выводе"),
    ("duplicate_paras",  "♻️ дубль абзацев"),
    ("format_broken",    "📐 нарушен формат"),
    ("invented_fact",    "🎩 выдуманный факт"),
    ("methodology_talk", "🧪 язык методологии"),
    ("too_long",         "📏 слишком длинно"),
    ("not_newsworthy",   "🥱 новость не стоит поста"),
    ("wrong_terms",      "🔤 неверная терминология"),
    # Два кода добавлены после разбора всех 23 черновиков (31.08) — оба
    # описывают системные, а не стилистические дефекты:
    #   duplicate_event   — одна новость в обоих TG-каналах порождает ДВА
    #                       черновика (дедуп источников намеренно отключён),
    #                       живой случай: 4 черновика по VK за 16.07 (722/741/
    #                       742/744) и пара 1102/1104 по Газпрому за 30.07;
    #   self_contradiction — текст противоречит сам себе или соседнему
    #                       черновику по тому же сигналу. Живые случаи: 745/797
    #                       («нарастили чистый шорт» при перекосе +68,2%, то
    #                       есть чистом ЛОНГЕ) и пара 1102/1104 («разночтений
    #                       нет, скачок однозначно лонговый» против «в лонг или
    #                       в шорт — наши данные не разделяют» на ОДНОМ сигнале).
    ("duplicate_event",  "👯 дубль того же события"),
    ("self_contradiction", "🌀 противоречит сам себе"),
    ("other",            "✍️ другое (впишу текстом)"),
]
REVIEW_REASON_LABELS = dict(REVIEW_REASON_CODES)

# Этап 6 — репост-хайп TG-каналов (MarketTwits + newssmartlab). Изначально решение
# принималось на чекпоинте +90мин, потом +15мин (найдено 2026-07-15: у ВСЕХ 6
# реально хайповых постов fwd_15 уже был выше ×3 к медиане — ждать до +90 не
# требовалось НИ РАЗУ). Найдено 2026-07-16 (219 постов с fwd_3+fwd_15, 38
# promoted): на +8мин разделение классов почти как на +15мин, а на +3мин у
# promoted было видно только 46% итогового fwd_15-сигнала против 73% на +8 —
# т.е. решение на 3 минуте формально РИСКОВАННЕЕ. Вадим осознанно выбрал
# скорость (+3мин, MTP_CHECKPOINT_1_MIN=3) вместо более консервативного +8 —
# 5/6/7/8 минут остаются ТОЛЬКО измерением, чтобы если 3 минуты дадут слишком
# много ложных срабатываний/пропусков — было к чему откатиться без даунтайма.
# Baseline теперь считается на fwd_3, не fwd_15 (fwd_15/fwd_15_at остаются в
# схеме как история, новый код их не пишет).
# Baseline считается ОТДЕЛЬНО по каждому каналу (tg_channel_watch.channel) —
# репосты у разных каналов не сравнимы напрямую (разный размер аудитории).
# MTP_CHECKPOINT_*_TOLERANCE_MIN — окно допуска вокруг чекпоинта; крон должен
# успевать поймать окно [checkpoint, checkpoint+tolerance] между тиками —
# при плотных точках (3/5/6/7/8 мин, шаг 1 минута) крон учащён до */2 (было */5).
TG_HYPE_CHANNELS = ["markettwits", "newssmartlab"]
MTP_CHECKPOINT_1_MIN = 3            # decision-чекпоинт (был 8→15→90, откат к самому раннему)
MTP_CHECKPOINT_TOLERANCE_MIN = 5    # шире кроновского интервала (*/2), чтобы
                                     # decision никогда не промахивался мимо окна
MTP_CHECKPOINT_5_MIN = 5            # только измерение — запасные точки МЕЖДУ 3 и 8
MTP_CHECKPOINT_5_TOLERANCE_MIN = 3  # на случай отката decision-чекпоинта назад
MTP_CHECKPOINT_6_MIN = 6
MTP_CHECKPOINT_6_TOLERANCE_MIN = 3
MTP_CHECKPOINT_7_MIN = 7
MTP_CHECKPOINT_7_TOLERANCE_MIN = 3
MTP_CHECKPOINT_8_MIN = 8
MTP_CHECKPOINT_8_TOLERANCE_MIN = 5
MTP_HYPE_RATIO_MIN = 3.0            # ×N к медиане fwd_3 последних наблюдений — тот же
                                     # ATR-стиль порога, что и у OI/фондов (PUBLIC_RATIO_MIN)
MTP_BASELINE_WINDOW = 50            # сколько последних fwd_3-наблюдений берём для медианы
